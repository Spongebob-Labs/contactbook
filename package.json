{
  "name": "@contactbook/contactbook",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "validate:api": "bash scripts/validate-api.sh"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
