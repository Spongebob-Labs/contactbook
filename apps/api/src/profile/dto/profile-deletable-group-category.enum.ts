/** API enum for DELETE /v1/profile/me/groups — exposed in OpenAPI as ProfileDeletableGroupCategory */
export enum ProfileDeletableGroupCategory {
  WORK = "work",
  BUSINESS = "business",
  SOCIAL = "social",
  FINANCIAL = "financial",
}
