# Optional: browser-visible API base (no trailing slash on version path)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Optional: Supabase (only if you use @supabase/supabase-js in the app)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
